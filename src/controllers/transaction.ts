import type { Request as ExpressRequest, Response as ExpressResponse } from 'express';
import * as db from '../db';

export default class TransactionController {
  static async findTransactionsByAccount() {}
}
